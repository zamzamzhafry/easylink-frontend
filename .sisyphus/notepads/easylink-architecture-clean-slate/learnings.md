# Learnings

- 2026-03-28: Built a canonical 3-tier bridge (`admin`, `group_leader`, `employee`) without changing runtime guards by adding additive mapping helpers in `employee-auth-model` and `auth-session`.
- 2026-03-28: Protected-route/API inventory is broader than initial references; current guarded APIs include attendance, schedule, performance, machine, scanlog, users, groups, shifts, employee profile, admin migration, and schedule-revision endpoints.
- 2026-03-28: Legacy flags/labels still actively shape access (`privilege`, `is_admin`, `is_leader`, `is_hr`, `can_schedule`, `can_dashboard`, plus `leader/scheduler/viewer/hr` labels), so compatibility-first mapping is required before adapter consolidation.
- 2026-03-28: Sidebar nav and attendance API guards now rely on the new `authorization-adapter` helpers (`canSeeNavItem`, `canAccessAttendance`, `getAttendanceGroupIds`, `canManageAttendanceNotes`, `canAccessRawAttendance`) to preserve the original access surface from a single source.
- 2026-03-28: Task 3 clean-slate contract migration is additive-only: canonical `cs_*` entities plus `vw_compat_*` bridge views were defined without dropping or mutating legacy tables.
- 2026-03-28: Monthly prediction target source is now contract-defined as `group override -> global fallback` through `cs_monthly_prediction_target_*` tables and `vw_prediction_target_effective`.
- 2026-03-29: Auth contexts now expose `canonical_roles` derived from legacy flags and role labels for both NIP and PIN paths, letting downstream adapters interpret the tidy `admin/group_leader/employee` taxonomy without touching existing flag-based checks.
- 2026-03-29: Added lightweight in-memory `recordLegacyAuthFallbackHit` counter so we can monitor how often the legacy PIN path fires while the bridge remains compatible.
- 2026-03-29: Added explicit migration runtime flag resolvers (`policy source`, `data-source cutover`, `machine parity exposure`, `reporting interaction`) with compatibility-first defaults so behavior remains unchanged unless operators intentionally toggle a gate.
- 2026-03-29: Built a staged machine user polling pipeline that streams SDK chunks, records checkpoint/chunk metadata, validates rows, and upserts idempotently so long-running pulls can resume without crashing on bad data.
- 2026-03-29: Exposed migration gate snapshot in admin-only scanlog sync responses and the right ops sidebar to make rollout/rollback posture observable without changing runtime data paths.

- 2026-03-29: Task 7 acceptance rehearsed by running the dry-run machine polling job (checkpoint path exercised with zero rows) and simulating a malformed chunk to confirm validation isolates bad rows while allowing the rest to continue.
- 2026-03-29: Task 0 preflight confirmed `.mcp.json` already declares the chrome-devtools and next-devtools MCP servers, and both CLI `--help` invocations succeed, so the tooling surface is ready for downstream steps.
- 2026-03-29: Playwright baseline smoke against http://localhost:3001/ (fallback because port 3000 was occupied) drove the login guard, recorded expected favicon/API 401/404 noise, and the `npm run typecheck`/`npm run build` pipeline completed without errors.
- 2026-03-29: Task 1 acceptance can be closed deterministically by comparing guarded API handler inventory (`getAuthContextFromCookies` route handlers) against documented Protected API matrix entries; current inventory and matrix both resolve to 31 entries.
- 2026-03-29: Forbidden label audit for `\bhr\b|\bscheduler\b|\bviewer\b` is stable when scoped to policy files: matches are limited to compatibility mapping/ingestion artifacts, with zero standalone matches in `app/api` decision handlers.
- 2026-03-29: Task 2 acceptance completed by capturing deterministic nav snapshots and API gate probe outputs that reference the centralized adapter helpers; documented both `.sisyphus/evidence/task-2-ui-parity.json` and `.sisyphus/evidence/task-2-api-parity.txt` for later verification.
- 2026-03-29: Task 11 addenda now exposes a deterministic safe vs legacy vs projection delta report (`/api/scanlog/sync?report=delta`) with stable sampling, counts, warnings, and an embedded migration gate marker; the JSON is saved under `.sisyphus/evidence/task-11-reconcile.json` for future audits.

- 2026-03-29: Task 11 replay step now exposes `POST /api/scanlog/sync` action `replay_delta` that idempotently merges safe->legacy rows for a date window while reporting safe_not_in_legacy counts before/after; output captured as `.sisyphus/evidence/task-11-replay.json`.

- 2026-03-29: Task 11 gate action `check_gate` now evaluates safe_not_in_legacy delta against a configurable threshold and responds with machine-readable `blocked`, `reason_code`, `threshold`, and `unresolved_total`, with evidence in `.sisyphus/evidence/task-11-gate-block.txt`.
- 2026-03-31: Task 11 acceptance now closes: deterministic delta reporting, replay idempotence, and cutover gate artifacts are recorded under `.sisyphus/evidence/task-11-(reconcile|replay|gate-block).*` and the plan checklist is marked complete.

- 2026-03-29: Task 12 API parity added Task-12 aliases (`devinfo`, `scanlog_new`, `users_partial`) routed through existing safe operations and now supply `artifact_metadata` when invoked with those canonical names; unknown-action errors expose the expanded `supported_actions` list and new evidence file.

- 2026-03-29: Task 12 UI parity adds explicit cards for Task-12 aliases (devinfo, scanlog_new, users_partial) plus artifact summary views and controls for alias-specific parameters while reusing existing action handlers. Evidence captured in `.sisyphus/evidence/task-12-parity-flow.json`.
- 2026-03-29: Task 13 backend hardening keeps `canAccessRawAttendance` as the admin gate and adds append-only `tb_scanlog_review_mutation_audit` writes for `hide`, `unhide`, and `tag` so actor/time/reason(note) are preserved per mutation event without overwriting prior audit rows.
- 2026-03-29: Task 13 UI slice extends attendance review punch actions with admin-only taxonomy controls (`late|acceptable|invalid`) plus reason/note drafts, and all admin mutations now submit a unified payload (`action`, punch identity fields, `status`, `reason`, `note`) to the existing `/api/attendance/review` POST path.
- 2026-03-29: Task 13 admin tagging QA was blocked because the review table renders 89 rows yet no admin "Tag" buttons appear for any punch timeline, so setting taxonomy/reason/note cannot happen in the current data state.
- 2026-03-29: Retried Task 13 QA (admin001) and confirmed 30 expanded rows still exposed zero Tag buttons (`admin only` labels = 0, status/reason/note inputs hidden), reinforcing that the UI never enters the admin-action mode for this dataset.
- 2026-03-29: Final Task 13 QA attempt with 2026-03-27 to 2026-03-29 filters produced the same blockage (89 rows with punches, 0 Tag controls, inputs hidden), so the admin tagging UI never becomes available for the current attendance sample.
- 2026-03-29: Task 14 slice 1 added a dedicated `easylink_right_sidebar_collapsed` localStorage key in `AppShell` so right-ops fold state is tracked/persisted independently from the left nav collapse key.
- 2026-03-29: `AppShell` now passes `collapsed` + `onToggle` props into `RightOpsSidebar` while keeping admin-only rendering and existing theme source-of-truth behavior unchanged.
- 2026-03-29: Task 14 slice 2 now consumes `collapsed/onToggle` inside `RightOpsSidebar`, adds a keyboard-accessible fold toggle, and gates queue endpoint polling behind lazy accordion expansion so collapsed mode only refreshes attendance-review summary.
- 2026-03-29: Task 16 attendance UI now consumes role-scoped `cumulative_summary` + `prediction_context` directly from `/api/attendance` payloads (top-level when present, otherwise row-level fallback) and renders a read-only non-admin summary panel without new mutation controls.
- 2026-03-29: Task 18 foundation added reusable semantic UI primitives in `app/globals.css` (`ui-page-shell`, `ui-card-shell`, control/input/select, primary/secondary button, and table shell classes) that are additive and token-driven for both light/dark themes before any JSX migration.
- 2026-03-29: Task 18 localization foundation provides `lib/localization/ui-texts.js` with EN/ID dictionaries and a fallback helper so report strings can move to localized keys safely.
- 2026-03-29: Task 18 attendance readability pass replaced repeated utility-heavy wrappers in `app/attendance/page.jsx` with semantic primitives (`ui-page-shell`, `ui-card-shell`, `ui-control-*`, `ui-btn-secondary`, `ui-table-*`) without touching API/state/role-flow logic.
- 2026-03-29: Updated non-admin monthly prediction panel title in `app/attendance/page.jsx` to align Task16 QA wording (`Monthly Prediction Context`) while leaving logic unchanged.
- 2026-03-29: Task 18 machine readability slice migrated dense utility chains in `app/machine/page.jsx` to existing semantic globals (`ui-page-shell`, `ui-card-shell`, `ui-card-muted`, `ui-control-*`, `ui-btn-*`, `ui-table-*`) while preserving queue handlers, Task-12 parity cards, artifact rendering, and danger confirmation flow unchanged.

- 2026-03-29 F1 audit: treat plan checkbox state as non-authoritative unless backed by runtime evidence; Task 13 blocker propagated to Task 18 and final-wave readiness.
- 2026-03-29 F1 audit: classify compliance separately as implemented vs runtime-verified to avoid false pass on checked items.

#ZS|- 2026-03-29: Task 14 QA blocked because the dev server process was killed after the tooling timeout, preventing Playwright automation from exercising the right ops sidebar collapse persistence; recorded the limitation in `.sisyphus/evidence/task-14-fold-persist.json`.
#ZT|- 2026-03-29: Task 14 QA re-run on port 3032 still blocked because npm dev server stops when the tool command times out, so Playwright collapse verification could not complete.
#ZX|- 2026-03-29: Task 14 right sidebar collapse persistence verified on http://127.0.0.1:3033/; collapse state stayed folded across reloads via the `easylink_right_sidebar_collapsed` flag.
#ZB|- 2026-03-29: Theme toggle QA confirmed repeated dark/light toggles (6 cycles) on http://127.0.0.1:3033/ and observed stable UI with no remount-loop symptoms.

#PZ|- 2026-03-29: AppShell now emits structured fallback events (window `__easylinkThemeFallbackEvents` + `easylink:themeRemountFallback` event) whenever stale-theme detection triggers the one-shot remount so post-toggle telemetry can confirm the fallback evidence.
- 2026-03-29: Added a neutral AppShell auth-loading skeleton so the "Checking session" state now shows a light/dark-friendly shimmer while auth gating remains untouched.
- 2026-03-29: Task 18 light-mode table fix replaced dark-biased attendance row/text/button utilities in `components/attendance/attendance-table.jsx` with semantic/token-aligned classes (`ui-table-*`, `text-foreground`, `text-muted-foreground`, `border-border`) while preserving row actions and callbacks.
- 2026-03-29: Re-skinned `components/groups/create-group-modal.jsx` inputs/buttons with light/dark token-aware classes so transparent cancel actions remain readable without changing submit/validation/close behaviors.
- 2026-03-29: Schedule grid sticky header + first-column now use translucent token surfaces with backdrop blur (`bg-background/*`, `border-border`, `text-foreground/muted`) and today auto-scroll now aligns in multi-frame passes (motion-safe smooth) for more reliable initial positioning.
- 2026-03-29: Task 18 readability research captured WCAG 1.4.3/1.4.11 contrast minima (4.5:1 normal, 3:1 large and UI indicators) and translucent blur risks for the sticky header/table surfaces.

- 2026-03-29: Task 18 perf trace captured API budgets (users, report, machine, scanlog, scanlog/sync, scanlog/stream, attendance/raw) driven by `parsePaginationParams`, `toBoundedInt`, queue caps, and `DRILLDOWN_LIMIT`, plus helpers (`lib/pagination`, `lib/easylink-sdk-client`, `hooks/use-paginated-resource`) sanitizing payloads, while heavy UIs (right ops sidebar, machine/report/attendance/scanlog pages) rely on lazy fetch/pagination/streaming to keep log views responsive and budget-aware.
- 2026-03-29: Task 18 localization hardening now uses `AppShell` as the shared locale source-of-truth (`easylink_locale` localStorage key, safe EN/ID resolver fallback to `en`) and passes locale controls into `Sidebar` while exposing context for page-level consumers.
- 2026-03-29: `/report` no longer locks text to browser locale; it now consumes shared shell locale at runtime, so `getUIText` labels and request/fetch error strings react immediately when the sidebar EN/ID toggle changes.

- 2026-03-29: Captured App Router locale-switch research referencing Next.js docs for useRouter/useSearchParams/cookies alongside the existing EN/ID dictionary helper for Task 18 planning.
- 2026-03-29: Task 18 retry confirmed `app/report/page.jsx` now consumes `useAppLocale` as shared runtime source (`resolvedLocale` from context) with explicit `en` fallback when locale is missing/invalid.
- 2026-03-29: Task 18 must-fix attendance surfaces now reuse semantic primitives in `attendance-filters`, `note-modal`, and `table-shell` (`ui-card-shell`, `ui-control-*`, `ui-btn-*`, `ui-table-*`), replacing repeated slate/dark utility chains while preserving existing props/contracts and event handlers.
- 2026-03-30: Task 18 hardening finalized light token readability by shifting `--card`/`--popover` to off-white (`140 22% 98%`) and adding semantic readability helpers (`ui-readable-body`, `ui-readable-muted`) plus table text-size floors in `app/globals.css`.
- 2026-03-30: EN/ID coverage expanded beyond report into attendance/machine domains via `lib/localization/ui-texts.js` (`attendancePage.*`, `attendanceShared.*`, `machinePage.*`), with runtime lookup through `getUIText(..., locale)` from shared `AppShell` locale context.
- 2026-03-30: Semantic class adoption is now applied on additional key list surfaces (`employees-table`, `groups-list`, and right-ops accordion/action controls), reducing slate-heavy utility duplication and improving light/dark consistency.

- 2026-03-30: Task 18 retry closed remaining UI hardening gaps by extending semantic classes, adding incremental show-more rendering for right-ops queue rows and group member lists, and localizing newly touched attendance, machine, and sidebar surfaces through ui-texts.
- 2026-03-30: Generated required evidence artifacts (task-18-stress.log, task-18-ui-perf.json, task-18-i18n-readability.json) and re-verified npm run build plus npm run typecheck success in this environment.
- 2026-03-30: Task 18 code-gap retry localized remaining sidebar and machine literals by routing visible copy through `getUIText` keys (`sidebar.*`, expanded `machinePage.*`) while keeping handlers/API payloads unchanged.
- 2026-03-30: Machine queue polling churn was reduced by gating refresh interval, active-job polling, and scanlog EventSource behind `isAdmin && isPageVisible` checks.
- 2026-03-30: Light-mode pure-white overrides were hardened by replacing flagged `rgb(255 255 255 / ...)` surfaces for sidebar sections and auth cards with off-white token-aligned backgrounds.
- 2026-03-30: Task 18 i18n/readability evidence refresh (single-file pass) captured factual auth-first runtime probes on port 3060, with deterministic blocker records when all seeded credentials returned `/api/auth/login` 401.
- 2026-03-30: Re-ran single-file Task 18 i18n/readability evidence with seeded `password` credential and confirmed authenticated EN/ID locale switch observations on sidebar/machine labels plus authenticated route checks for `/attendance`, `/machine`, and `/report`.
- 2026-03-30: Task 18 stress evidence refresh now uses authenticated admin probes with bounded params (`machine limit=20`, `scanlog/report limit=200`) and logs per-endpoint status, elapsed ms, and response bytes.
- 2026-03-30: Authenticated UI perf sampling on `/attendance`, `/machine`, `/report` now records per-route navigation metrics and interaction p50/p95; report filter interactions showed the largest latency spikes.
- 2026-03-30: Regenerated stress evidence with authenticated admin cookie-jar probes on port 3065 and captured status+ms+bytes for auth/me, machine, scanlog, and report bounded endpoints.
- 2026-03-30: Scoped UI perf measurement for Task18 machine/log-heavy flows (without report route) reached 74 samples and produced passing aggregate p95 under the 100ms threshold.
- 2026-03-31: Task 12 criterion #1 parity can be proven deterministically without UI refactor by auditing source markers: approved aliases (`devinfo`, `scanlog_new`, `users_partial`) from `route.js`, corresponding PS1 functions (`Get-DeviceInfo`, `Get-ScanlogNew`, `Get-UserAllStream`), and card/action wiring in `app/machine/page.jsx`; refreshed `.sisyphus/evidence/task-12-parity-flow.json` with this static parity matrix.
- 2026-03-31: Task 12 criterion #2 highlights that each alias now owns its own normalizeAction branching plus runMachineAction handler; the new `scripts/task12-mapping-audit.mjs` command verifies the bijective card↔API mapping before regenerating the evidence file.
- 2026-03-31: Task 12 criterion #3 shows the streaming user pagination path surface chunk progress metadata, write partial-save checkpoints per chunk, and stay within the bounded machine queue (`queueMeta` + `MACHINE_WORKER_CONCURRENCY`); the `EASYLINK_MACHINE_POLLING_DRY_RUN=1 node --input-type=module -e "import { runMachineUserPollingJob } from './lib/machine/pull-users-pipeline.mjs'; ..."` command documents the dry-run summary even when MySQL refusing connections.
- 2026-03-31: Task 13 criterion #1 unblocks admin tagging by short-circuiting `canReviewPunch` for admins before legacy `can_review` flags are evaluated, preserving non-admin gating while keeping status/reason/note controls always visible to admins.
- 2026-03-31: Task 13 criterion #2 can be closed with static proof only: `hide`, `unhide`, and `tag` each call `appendReviewMutationAudit`, and the insert path always persists `action`, `actor_pin`, and `acted_at` (with tag also carrying `status`/`note`, and hide-family carrying `reason`).
- 2026-03-31: Task 13 criterion #3 remains centrally enforced because `POST /api/attendance/review` checks `canAccessRawAttendance(auth)` before any `hide`/`unhide`/`tag` branch, so one admin-only gate covers all mutation actions.
- 2026-03-31: Task 14 criterion #1 refresh now initializes right-sidebar collapse state from `easylink_right_sidebar_collapsed` during `AppShell` state setup (with login-route guard), preventing first-render flicker while preserving the same stable storage key and toggle persistence contract.
- 2026-03-31: Playwright re-verification on `/attendance` confirmed admin right sidebar collapses, writes `easylink_right_sidebar_collapsed=1`, and stays collapsed after reload; evidence refreshed at `.sisyphus/evidence/task-14-fold-persist.json`.
- 2026-03-31: Task 14 criterion #2 is validated with fetch-interception over 17s windows: collapsed sidebar produced zero matched polling calls while expanded sidebar resumed review polling; evidence captured at `.sisyphus/evidence/task-14-polling-collapse.json`.
- 2026-03-31: Task 14 criterion #3 is validated with on-demand accordion fetch checks: with detail accordions closed, scanlog/machine detail calls stayed at zero, and expanding the accordions triggered both detail polling paths; evidence at `.sisyphus/evidence/task-14-accordion-demand.json`.
- 2026-03-31: Task 14 criterion #4 static audit confirms theme authority is root-level (`AppShell` + `html[data-theme]` + global CSS tokens) and `RightOpsSidebar` consumes semantic tokens without local theme ownership; evidence at `.sisyphus/evidence/task-14-theme-root.json`.
- 2026-03-31: Task 14 criterion #5 forced a single theme mismatch check and observed exactly one `easylink:themeRemountFallback` event with one fallback record append, validating one-shot remount/event behavior; evidence at `.sisyphus/evidence/task-14-theme-fallback.json`.
- 2026-03-31: Task 15 criterion #1 static proof confirms non-admin users cannot see Machine nav entry because sidebar item requires `auth:'admin'` and nav filtering uses `canSeeNavItem`; evidence at `.sisyphus/evidence/task-15-nonadmin-machine-guard.json`.
- 2026-03-31: Task 15 criterion #2 confirms `/machine` route guard in AppShell redirects non-admin sessions to `/dashboard` (`pathname === '/machine' || startsWith('/machine/')`), with static+prior-runtime proof in `.sisyphus/evidence/task-15-route-nav.json`.

- 2026-05-22: Task 16 scope partition now routes schedule access through authorization-adapter helpers so leader can manage schedule while employee can open group schedule read views without exposing attendance review controls.

- 2026-05-23: Task 18 readability hardening landed cleanest when semantic wrappers ('panel-card', 'table-shell', 'control-*', 'btn-*') delegate back to existing 'ui-*' primitives instead of redefining a second style layer; duplicate semantic definitions in globals can drift and should stay thin aliases.
