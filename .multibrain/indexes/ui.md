# UI Bucket

Newest first.

- `2026-06-25 23:30` — @claude — Full i18n (13 pages EN/ID) + UI features. Wired dashboard(+DashboardCharts/DashboardNeedsReview children), scanlog, employees, schedule, groups to `useAppLocale`/`getUIText`; RSC dashboard reads locale via new `getLocaleFromCookies()` (`lib/auth-session.ts`), app-shell mirrors locale to cookie. ui-texts +5 page key blocks (en+id). viewMode dead toggle → live (`app/users` passes view={viewMode} to DataTable; `use-persisted-preference` syncs same-document via `easylink:pref` custom event — storage event only cross-tab). Sidebar: changelog modal (version btn → ModalShell + CHANGELOG const) + footer accordion (Preferences/Account sections matching top-nav pattern). Schedule header: 4 stacked control panels → 1 condensed card with dividers+icons. Analytics: formula info modals (Info icon per StatCard + Formula btn per chart → ModalShell with Formula/Logic/Impact, 8 metrics). Optimistic shift update (`setShift` patches cell via setData, no full reload — verified POST with zero GET after). Residual-string audit caught schedule `selectedGroupLabel` hardcoded "All Groups" → fixed. -> .multibrain/context/2026-06-25-2330-claude-php8-sdk-pivot.md
- `2026-06-24` — @claude — Improvement backlog captured for off-screen loop work. Bug-hunt complete (18+ fixed, 6 clean scan iters). Forward backlog: P1 split 4 complexity hotspots (attendance 1706 LOC, ui-texts 1561, machine 1467, schedule 1264), test 4 untested security libs (id-holidays-fallback, easylink-sdk-client, hop-b-ingest-handler, auth-audit), pre-commit token grep gate. P2: a11y audit (67 flagged likely false-positives), saveCustomHolidays race, config passthrough zod. Runtime regression fixed (inferShiftIconKey dup def — tsc missed, runtime caught). Established patterns: transaction finally-release, resolveDateRange 366d cap, shared-lib + tests, semantic tokens only, runtime smoke verify. -> .multibrain/context/2026-06-24-improvement-backlog.md

- `2026-06-24` — @claude — Comprehensive UI bug hunt session: Fixed 18+ bugs (security: CSV injection guard, SQL parameterization, open redirect, auth bypass; performance: connection leaks, machine double-poller, SSE stream leak, date-range validation on 7 routes; correctness: print XSS, format date UTC bug, silent failures, partial delete; UX: bulk-assign validation, silent popup block). Created 4 shared utility libs (csv.js, time.js, date-range.js, format-date.js) with 24 tests. Tailwind token migration (226 violations cleared across 37 files). Fixed dark mode config (darkMode: ['selector', '[data-theme="dark"]']). Added compact print mode for schedules (symbols vs full-length). Invoked interview system designer skill for Senior SWE role. Plan progression v1→v7 tracking improvements. -> .multibrain/context/2026-06-24-bug-hunt-session.md

- `2026-06-13 11:50` — @sisyphus — Two UI bug fixes from user report. (1) React KEY WARNING at /attendance + /scanlog ('Each child in a list should have a unique key prop. Check render method of AttendanceFilters'): ROOT CAUSE in `components/attendance/attendance-filters.jsx` preset-button map referenced `PRESET_RANGE.today/.week/.month` but `lib/attendance-helpers.js` exports `PRESET_RANGE` as an ARRAY of `{key,label}` (today/week/month/last) — so `.today/.week/.month` were ALL `undefined` → `key={undefined}` (the warning) AND broken `onSetRange(undefined)`. FIX: `PRESET_RANGE.filter(p=>p.key!=='last').map(preset => <button key={preset.key} type="button" onClick={()=>onSetRange(preset.key)} className="pill-button px-3 py-1.5 text-xs">{presetLabel(preset.key, preset.label)}</button>)`. `handleSetRange(unit)` (app/attendance/page.jsx L489-499) expects 'today'/'week'/'month' string. Filtered out 'last' to preserve original 3-button UX. Verified Playwright: 0 key warnings, 3 buttons render (Today/This Week/This Month). (2) SCANLOG canonical-empty UX: user 'canonical and legacy data still on legacy'. ROOT CAUSE: `tb_scanlog_safe_events` (canonical Hop-B store) = 0 rows, `tb_scanlog` (legacy) = 66,441 rows. Code is CORRECT (existence-based source resolver `lib/scanlog-read-source.js`); canonical store was never populated (SDK ingest never ran) — data/ops gap NOT a code bug. User chose empty-state UX only (no backfill/mutation/default-flip). FIX in `app/scanlog/page.jsx`: added `<InlineStatusPanel variant="warning" message={appliedFilters.source==='canonical' && !loading && !loadError && records.length===0 ? 'Canonical store has no events yet (SDK ingest pending). Switch Source to "Legacy Scanlog Table" to view historical scan data.' : null} />` between error panel and table. Uses appliedFilters.source (queried) not source (pending dropdown). Verified Playwright admin001→/scanlog: hasBanner=true. typecheck clean. -> committed same session with login-loop fix.

- `2026-06-12 14:30` — @sisyphus — D1 mobile drawer "bug" RESOLVED as FALSE ALARM. Verified functionally correct in PROD build (next build+start on :3000): synthetic React click → aria-expanded=true, aside translate-x-0 left=0 w=240, backdrop present, 13 expanded nav links; backdrop-click closes (aria=false, aside -216); reopen works; screenshot confirms full drawer over dimmed main. Earlier "trusted click fails" was a Playwright/CDP input-injection failure in the MCP session — mouse.click/down/up + keyboard.press deliver ZERO events to ANY element (document capture listener saw nothing on body & h1 clicks too); only synthetic dispatchEvent/JS handler calls work. NO code change needed. app-shell.jsx + sidebar.jsx drawer wiring is correct.

- `2026-06-11 20:00` — @sisyphus — Server topology: pm2 'easylink-frontend' (id 0) served DEPLOYED copy /home/user/apps/easylink-frontend on :3000 (NOT workspace). Stopped it (pm2 stop, reversible) to free :3000; started workspace dev server (~/projects, tmux session 'elworkspace', log /tmp/el-workspace-dev.log) so wave1-3 UI is now live on :3000. Copied deployed .env→workspace (gitignored; DB creds + HOP_B_AUTH_TOKEN). HOP B ingest verified live end-to-end: handshake 400 BATCH_EMPTY, valid batch 200 BATCH_ACCEPTED inserted=1, replay 200 BATCH_REPLAYED; DB ledger+canonical row confirmed; synthetic VERIFYBOX rows cleaned. 34/34 hop-b unit tests pass. auth/me 401 + login POST 400 behave correctly on both copies — specific 'auth blocked' symptom still needs repro from user.

- `2026-06-11 17:00` — @sisyphus-junior — C5 i18n: Added shiftsPage.table.{loading,empty}, analyticsPage.bradford.empty, performancePage.{daily,topLate,groupPunch,table}.{empty/loading} in both en+id in ui-texts.js; shifts/analytics/performance pages import getUIText+useAppLocale, add resolvedLocale+t hook, replace 7 hardcoded JSX strings. eslint clean.
- `2026-06-11 16:30` — @sisyphus-junior — D1 mobile sidebar drawer: app-shell.jsx adds mobileOpen state (not persisted) + fixed hamburger (Menu icon, h-11 w-11, z-50, lg:hidden, aria-label/expanded) + main now `ml-0 lg:ml-{20,60}`; sidebar.jsx gains mobileOpen/onMobileClose props, effectiveCollapsed=`collapsed && !mobileOpen` (drawer always expanded), aside transform `-translate-x-full lg:translate-x-0` / `translate-x-0 z-50 w-60` when open, backdrop <button> (fixed inset-0 z-40 bg-black/50, aria-label close), Escape-key close + usePathname-change close. Desktop behavior unchanged. App class names intact. eslint clean. Right-ops-sidebar untouched.
- `2026-06-11 16:30` — @sisyphus-junior — D1 mobile drawer: app-shell.jsx hamburger (lg:hidden, 44px, Menu, aria-expanded) + mobileOpen state + main ml-0 lg:ml-20/60; sidebar.jsx transform drawer (-translate-x-full lg:translate-x-0), backdrop button, Escape + usePathname close, effectiveCollapsed=collapsed&&!mobileOpen forces expanded nav. Desktop unchanged, .app-sidebar* classes intact. eslint clean.
- `2026-06-11 16:30` — @sisyphus-junior — C5 i18n: lib/localization/ui-texts.js new namespaces shiftsPage/analyticsPage/performancePage (en+id); shifts(2)/analytics(1)/performance(5) hardcoded empty/loading strings → useAppLocale+getUIText t(). eslint clean.
- `2026-06-11 16:00` — @sisyphus — Wave-3 pre-check: D5 right-sidebar overlay, E1 report nav (admin-gated, present), E3 review presets/anomaly filters, recovery-panel manual-only fetch — ALL already fixed in current build; QA tested stale build. Remaining real work delegated: D1 mobile drawer (sidebar.jsx+app-shell.jsx), C5 i18n empty/loading strings (shifts/analytics/performance). C7 machine polls deferred (no device).
- `2026-06-11 12:30` — @sisyphus-junior — sidebar.jsx: A2 aria-labels (toggle/settings/theme/view/locale/logout), A3 aria-current=page on nav links, B1 slate-500/600→muted-foreground, B2 text-[10px]/[11px]→text-xs
- `2026-06-11 12:30` — @sisyphus-junior — unassigned-panel + DashboardNeedsReview: slate→semantic tokens, div list→ul/li (A6); deleted dead root /layout.jsx (D7)
- `2026-06-11 15:00` — @sisyphus-junior — attendance/page.jsx: C2/C3 fix — main load() failure sets loadError + InlineStatusPanel(error, Retry=load) above summary table, cleared on success/refetch; groups & holidays .catch keep [] / {} fallback but each fires one-shot warning() toast (groupsWarnedRef/holidaysWarnedRef de-dup, reset on success). i18n keys added attendancePage.errors.* (en+id) in lib/localization/ui-texts.js. quick-summaries untouched. eslint clean.
- `2026-06-11 14:00` — @sisyphus-junior — analytics+performance/page.jsx: C2/C3/C4 fix — main fetch failure sets loadError + InlineStatusPanel(error,Retry=load) above data; prefetch .catch keeps [] but fires warning() toast; data area wrapped in aria-busy div w/ opacity-60 pointer-events-none transition-opacity while loading. eslint clean.
- `2026-06-11 13:00` — @sisyphus-junior — schedule/page.jsx: C2/C3 fix — main GET failure now sets loadError + InlineStatusPanel(variant=error, Retry=load) above grid; holiday .catch keeps empty-map but fires warning() toast
- `2026-06-11 13:00` — @sisyphus-junior — Fixed C2 silent dashboard fetch failures in app/page.jsx: each .catch now records failedSections; inline amber warning banner renders near top when any read fails (RSC, fallback values kept)
- `2026-06-11 12:00` — @sisyphus-junior — Fixed form-label a11y (A1/A4) + B1/D2 slate→token in edit-employee, settings, bulk-assign modals + search-input aria-label
- `2026-06-11 00:00` — @sisyphus-junior — Fixed 4 audited table UI bugs (D3 overflow, D4 dup delete, D6 long names, C1 loading skeleton) -> .multibrain/context/2026-06-11-ui-table-bug-fixes.md
