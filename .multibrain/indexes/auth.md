# Auth Bucket

Newest first.

- `2026-06-13 10:45` — @sisyphus — Task 16 (break-glass reassign per Amendment A) DONE. Canonical NIP-lane break-glass = `admin001`/`password` → karyawan_id=10006 (active alpha-NIP, role admin/NULL, k.nip='admin001' outside placeholder block 9990001-9990044). Decommissioned `ADMIN01`/kar9999 because it is `is_active=0` AND its k.nip=9990044 sits INSIDE the placeholder block — reactivating would self-lockout via isPlaceholderEmployeeNip guard. Verified live: `curl POST /api/auth/login {login_id:"admin001",password:"password"}` → HTTP 200 + `{karyawan_id:10006, is_admin:true, subject_type:"employee_nip"}`; cookie `{"sub":"10006","st":"karyawan_id","v":2}` (T9 canonical); `/api/auth/me` 200 + `is_admin=true`. Account-lane fallback `admin01`/`Admin@123` still works with `st='account'` cookie (auth_accounts has no karyawan_id FK; T17 retires lane). Doc updates: `docs/auth-nip-reanchor/04-db-ground-truth.md` (new break-glass section). Human QA runbook: `.omo/evidence/auth-nip/t16-human-qa-runbook.md` with 6 scenarios (admin001, leader001, employee001, admin01, placeholder-block negative, logout) covering /me JSON expectations + cookie payload decode + sidebar/page view assertions per role. No code changed (typecheck still clean). Plan checkbox [~]→[x] at L936. Amendment A spine progress: 13/17 done; remaining T4, T13 (12h wait), T14, T15, T17.

- `2026-06-13 10:05` — @sisyphus — Task 12 (dual-lane runtime verification gate) DONE. 3 scenarios all HTTP 200 in same window via fresh-cookie curl: (1) employee001/password NIP lane → karyawan_id=10008, is_admin/is_leader=false, viewer grp32 (can_dashboard only), subject_type='employee_nip'; (2) leader001/password NIP lane → karyawan_id=10007, is_leader=true, can_schedule=true, group_leader on grp32 only, canonical_roles=['group_leader','employee']; (3) admin01/Admin@123 account lane → account_id=1, role_key='admin', is_admin=true, groups=[]. Cookie payload decode confirmed: NIP cookies {st:'karyawan_id', sub:numeric, v:2} (T9 canonical), account cookie {st:'account', sub:'admin01', v:2} (intentional per Amendment A — auth_accounts has no karyawan_id FK; T16 retires lane). SQL gate: `SELECT COUNT(*) FROM tb_karyawan WHERE nip IS NULL` = 0. False-alarm in first runner v1 (broken jq filter showing null /me fields) misdiagnosed as `getAuthContextFromCookies` await regression — direct raw-output curl proved decode works perfectly; runner v2 rebuilt + all green. Code-hygiene note carried: explicit `await` on 8 `return createAuthContext*` sites in waterfall (lib/auth-session.ts:890/897/901/905/912/915/919/929) would improve readability but is NOT a bug (async function unwraps inner Promise). Evidence: .omo/evidence/auth-nip/t12-dual-run.txt (213 lines). Step-3 unit (T9+T12) ready for commit. T10 CANCELLED per Amendment A.

- `2026-06-13 09:30` — @sisyphus — Task 9 (H1) DONE per Amendment A: NIP-lane session subject is now numeric `karyawan_id` (immutable PK), account lane stays on `account:` prefix because `auth_accounts` table has NO `karyawan_id` FK (verified via `SHOW CREATE TABLE auth_accounts` — no karyawan_id column; Amendment A T16 will retire standalone account lane). Edits: (1) `lib/auth-hardening-helpers.js` `normalizeSubjectType` accepts new `karyawan_id` lane; (2) `lib/auth-session.ts` `AuthSubjectType` union extended; (3) `decodeSession` inferredSubjectType adds `/^\d+$/` → `karyawan_id` for prefix-less numeric subjects; (4) `getSubjectValueForType` adds karyawan_id case (bare value, no prefix strip); (5) `getAuthContextFromCookies` waterfall: canonical numeric-karyawan_id branch FIRST and UNGATED → `createAuthContextByKaryawanId(Number(sub))`, then explicit-typed `account`/`employee_nip`/`legacy_pin` and prefix-inferred legacy branches GATED by `EASYLINK_ENABLE_LEGACY_SESSION_PAYLOAD_COMPAT` (T13 flips OFF after 12h soak); (6) `app/api/auth/login/route.js` `finalizeLoginSuccess` writes numeric karyawan_id subject + `st='karyawan_id'` ONLY when `subjectType==='employee_nip'` AND `authContext.karyawan_id` is positive int; account lane falls back to existing `loginId` + `st='account'` behavior; (7) `tests/auth-hardening.test.js` `normalizeSubjectType` test extended to assert karyawan_id accepted. Token wire format already v:2 (no version bump needed). Gates: `npm run typecheck` EXIT 0; 5-file auth test suite 34/35 PASS (1 fail = pre-existing `ALLOW_INSECURE_COOKIES=true` env baseline in auth-session-compat — unrelated). Live QA: (a) NIP login leader001 → decoded cookie `{"sub":"10007","st":"karyawan_id","exp":...,"v":2}`, /api/auth/me 200 + karyawan_id=10007; (b) account login admin01 → decoded `{"sub":"admin01","st":"account",...}`, /api/auth/me 200 + is_admin=true + account_id=1; (c) forged legacy `nip:leader001` cookie HMAC-signed with AUTH_SECRET → /api/auth/me 200 + karyawan_id=10007 (compat path); (d) forged legacy `account:admin01` → /api/auth/me 200 + admin context. Evidence: `.omo/evidence/auth-nip/t9-numeric-subject.txt` (3 scenarios) + `.omo/evidence/auth-nip/t9-legacy-compat.txt` (2 forged-cookie scenarios). Comments in code: `lib/auth-session.ts` waterfall has 3 marker comments documenting canonical-vs-compat semantics + T13 dependency; `app/api/auth/login/route.js` finalizeLoginSuccess has 1 marker comment documenting auth_accounts schema constraint + T16 dependency — necessary because asymmetric subject handling is non-obvious. NOT committed (CONTEXT.md no-auto-commit). Caught-during-impl: initial design refused account-lane login entirely when karyawan_id missing — would have broken break-glass admin01. Asymmetric write fixed before tests.

- `2026-06-13 03:25` — @sisyphus-junior — F3 Real Manual QA executed (final wave reviewer audit) for IMPLEMENTED auth-NIP scope: 14/14 testable scenarios PASS, 5 scenarios deferred behind blocked tasks (T9/T14/T15/T16/T17). VERDICT: APPROVE-FOR-COMPLETED-SCOPE. Coverage: (1-4) NIP-lane curl logins admin001/leader001/employee001 + wrong-pw (200/200/200/401 byte-exact 'Invalid credentials'); (5) placeholder NIP 9990001 via temp-seed kar1 auth.nip=QA_PH_F3 → 401 byte-identical to wrong-pw (unified `invalidCredentialsResponse()`), CLEANED UP; (6) rate-limit: 10x 401 then 11th=429 with `retry-after: 60` + `x-ratelimit-limit: 10`; (7-9) admin password reset 5-step cycle: admin login → reset emp→TempPass123 (200) → emp login temp (200) → reset back to 'password' (200) → emp login orig (200 restored), audit table `tb_password_reset_audit` got 2 new rows id 8+9 actor=10006/target=10008, plus non-admin reset → 403 'Forbidden', unauth → 401 'Login required.', employee001 hash prefix still $2b$; (10) GET /api/groups admin001 → top-level `leaders` array length 5 matching DB tb_karyawan_roles exactly incl grp9 multi-leader kar29+kar108; (11) assign_leader+remove_leader for leader001/grp9 → audit `tb_role_change_audit` 1 grant + 1 revoke row, actor=10006 target=10007; (12-14) Playwright UI role-nav after fresh login via fetch+navigate: employee001 sees only `/`,`/schedule` (no admin), leader001 sees `/`,`/schedule`,`/attendance`,`/performance`,`/analytics` (no admin), admin001 sees ALL 13 nav items. CLEANUP confirmed: kar1 auth rows=0, no QA_PH* residue anywhere, employee001 still authenticates with original 'password'. Evidence: `.omo/evidence/auth-nip/F3-manual-qa.md` + `.omo/evidence/auth-nip/final-qa/{s1-s4-curl,s5-placeholder,s6-ratelimit,s7-s9-admin-reset,s10-s11-groups,s12-s14-ui-nav}.txt`. Gotchas (added to learnings): .next cache corruption needs `rm -rf .next && npm run dev`; bash heredoc with embedded quoting needs script file; `set -u` + bash `local` triggers unbound-var; `/api/groups` returns flat top-level `leaders` array NOT nested; Playwright MCP controlled-form requestSubmit STILL didn't bind React state, workaround = page.evaluate fetch+then browser_navigate; middleware 30/min IP rate limit needs ~60s cooloff after rate-limit burst tests. NO code modified, NOT committed.

- `2026-06-13 03:04` — @sisyphus-junior — Task 19 (M4) DONE: admin-only password reset for NIP lane (NO self-service). New POST `app/api/admin/password-reset/route.js` (thin Next adapter: getAuthContextFromCookies→parse body→delegate) + pure core `lib/admin-password-reset.js` `handleAdminPasswordReset({auth,body,pool})`: 401 if !auth ('Login required.'); 403 generic 'Forbidden' if auth.is_admin!==true (no admin-probe leak); zod {target_karyawan_id coerce int>0, new_password 8..200}→400 'Invalid input'; hashPassword bcrypt; parameterized `UPDATE tb_karyawan_auth SET password_hash=? WHERE karyawan_id=?`; affectedRows===0→400 'Target auth account not found'; recordPasswordReset(actor=auth.karyawan_id??null) on same connection; 200 {ok:true}. AUDIT = NEW sibling table `tb_password_reset_audit` (scripts/migration-task-19-password-reset-audit.sql, additive, id/actor_karyawan_id NULL/target_karyawan_id/created_at, 2 idx, NO password material) — chosen over ENUM-ALTER of tb_role_change_audit to avoid coupling with blocked T4 enum-narrow. `lib/auth-audit.ts` gained `recordPasswordReset({actorKaryawanId,targetKaryawanId},executor=pool)` sibling. RATE-LIMIT: none by design (admin-only behind cookie, no cred-guess surface, middleware 30/min per-IP covers /api/*) — documented in headers. tests/admin-password-reset.test.js 5/5 PASS (401/403/400/200+bcrypt+audit/400-not-found) via `node --import tsx --env-file=.env --test`. typecheck EXIT 0; build ✓Compiled+types valid+27/27 static EXIT 0 (font fetch warns env-only). Live QA (evidence task-19-password-reset.txt): admin reset emp→temp 200; emp login temp 200; emp orig 401; non-admin reset 403; unauth 401; reset back 200; emp orig 200 RESTORED; audit 2 rows actor=10006 target=10008; hash $2b$. employee001 pw restored to 'password', no leftover seed rows. NOT committed; no ALTER legacy; auth.nip/login/break-glass untouched.

- `2026-06-13 03:01` — @sisyphus-junior — Task 11 (H4) DONE: groups leader READ+WRITE re-anchored from tb_user_group_access.is_leader (pin) -> tb_karyawan_roles (karyawan_id, role_key='group_leader', group_id). Migration scripts/migration-task-11-leader-backfill.sql (idempotent INSERT...SELECT...WHERE NOT EXISTS, applied: group_leader rows 1->5, +4 backfilled, re-run stays 5). app/api/groups/route.js: GET leader read now JOINs tb_karyawan_roles->tb_karyawan (projects pin/nip/nama/karyawan_id, k.isDeleted guard) L104-119; assign_leader INSERTs roles row (multi-leader-safe NOT EXISTS) L175-232; remove_leader DELETEs roles row L234-273; recordRoleChange grant/revoke kept; device-table writes RETAINED column-gated as DEAD DATA (never read). Multi-leader/group confirmed (grp9 kar29+kar108). tsc EXIT 0; build fails only on Google-Fonts ETIMEDOUT (env baseline). Live admin001 QA: list==roles ground-truth, assign->roles+audit grant+API multi-leader, idempotent re-assign no dup, remove->roles gone+audit revoke+API single, device row dead-data flip restored. Evidence .omo/evidence/auth-nip/task-11-groups-leader.txt. NOT committed. NO ENUM alter, NO login/auth.nip touch.

- `2026-06-13 03:00` — @sisyphus-junior — Task 8 (ADDITIVE) DONE: added `createAuthContextByKaryawanId(karyawanId, connectionParam=null)` to lib/auth-session.ts (fn L639, SELECT L653) — resolver keyed by tb_karyawan.id for the karyawan_id session subject (decoded in T9). Mirrors createAuthContextByNip EXACTLY (B2 global-only admin/hr, B1 per-group is_leader, tb_user_group_access fallback, identical return incl subject_type='employee_nip'); ByNip itself UNCHANGED. SQL parameterised one `?`: `... WHERE k.id = ? AND a.is_active = 1 AND k.isDeleted = 0` (new lane carries isDeleted=0 from day 1; NIP-lane resolvers get it at T17). Reuses isPlaceholderEmployeeNip(karyawan_nip) post-resolve guard (defense in depth, no matcher dup). Gates: typecheck (tsc --noEmit) CLEAN; build CLEAN (network up this run, full 27-page table). Live (tsx, DB user=easylink TCP — root socket denied): kar10007 leader001→AuthContext is_leader=true groups=[{32,is_leader:true}]; kar10004 is_active=0→null; kar1 (k.nip=9990001 placeholder, temp-seed auth nip=QA_PH_T8 active)→null via placeholder guard, seed DELETE'd→kar1 count 0 baseline. Dev restarted post-build (.next wiped), warm GET /=200 /login=200. Evidence .omo/evidence/auth-nip/task-8-resolver-by-id.txt. NO change to subject/cookie (T9), auth.nip drop (T10), login route, error strings, break-glass. NOT committed.

- `2026-06-13 02:55` — @sisyphus — Task 5 (M3) RE-VERIFIED on resumed run (no rework). All deliverables intact: scripts/migration-task-5-role-change-audit.sql (additive tb_role_change_audit, 3 indexes, InnoDB utf8mb4, applied — DB COUNT=3 rows from prior test), lib/auth-audit.ts (recordRoleChange, executor?=pool, parameterised, validated, no as-any), app/api/groups/route.js (import L11, LEADER_ROLE_KEY L13, resolveKaryawanIdByPin L15-21, assign_leader grant L199-205, remove_leader revoke L234-240). Gate: `npm run typecheck` EXIT 0 CLEAN (authoritative TS gate). `npm run build` fails on Google-Fonts ETIMEDOUT (no network in sandbox) BEFORE app compile — environmental, not a regression. Evidence .omo/evidence/auth-nip/task-5-audit.txt. Verification-only, NOT committed.

- `2026-06-13 03:10` — @sisyphus — Task 7 RE-VERIFIED on current tree (block already applied prior session, code unchanged this run). Consts `PLACEHOLDER_NIP_MIN=9990001`/`PLACEHOLDER_NIP_MAX=9990044` + `isPlaceholderEmployeeNip()` (digit-only `/^\d+$/`+Number.isInteger+range, doc-link /tmp/nip_placeholder_report.tsv) in lib/auth-session.ts. Two guards: app/api/auth/login/route.js L137 (NIP lane, BEFORE verifyPassword, unified `invalidCredentialsResponse()` 401) + lib/auth-session.ts createAuthContextByNip L524 (returns null → /api/auth/me drops placeholders). Both SELECTs carry `k.nip AS karyawan_nip`; WHERE still `a.nip=?`. Gates: typecheck (tsc --noEmit) CLEAN; node --test tests/auth-session-compat.test.js 8/8 PASS. `npm run build` ✓Compiled+types valid+27/27 pages but post-compile collect-build-traces ENOENT `_not-found/page.js.nft.json` = ENVIRONMENTAL offline-fonts (fonts.gstatic.com), same baseline class as L68/L108, NOT a code regression. Live re-verify (temp-seed kar1 auth.nip=QA_PH_T7 hash←employee001, k.nip=9990001 placeholder): placeholder→401 byte-identical to employee001+WRONGPW→401; employee001→200; DELETE temp → kar1 auth count 0 baseline restored. Boundary/matcher 19-case table (prior evidence) all PASS: 9990000/9990045 NOT blocked, alpha admin001/leader001/employee001 + short 9999/99999 unaffected. Evidence appended task-7-placeholder.txt. Dev server restarted (build wiped .next). NOT committed.

- `2026-06-13 02:50` — @sisyphus — Task 6 (H5) DONE: per-(ip+login_id) sliding-window rate-limit (10/60s) at `lib/auth-login-rate-limit.js`, wired into `app/api/auth/login/route.js` BEFORE credential check. All 4 credential-failure 401s (account-bad-pw, nip-no-row, nip-placeholder, nip-bad-pw) unified through `invalidCredentialsResponse()` const — byte-identical bodies, no user enumeration. CSRF stays OUT (LAN-only + middleware same-origin) — one-line comment marks H5 rationale. Coverage: middleware = 30/min/IP coarse, this = 10/min/(IP+loginId) fine. Tests: tests/auth-login-rate-limit.test.js (5 pass). npm run typecheck + npm run build clean. Real-login QA admin001/leader001/employee001 still 200. Evidence: `.omo/evidence/auth-nip/task-6-ratelimit.txt` (11x bad cred → 11th=429 + identical bodies diff). Gotchas: `npm run build` wipes .next so `next dev` must be restarted; `git stash push -u` lost untracked new files — avoid. Concurrent WIP from other agent (`isPlaceholderEmployeeNip` placeholder block) already in working tree; my unified-error helper wraps its branch too.

- `2026-06-13 02:45` — @sisyphus — Task 5 (M3) role-change audit DONE: additive migration scripts/migration-task-5-role-change-audit.sql creates tb_role_change_audit (id PK AI, actor_karyawan_id INT NULL, target_karyawan_id INT, action ENUM grant/revoke, role_key VARCHAR(64), group_id INT NULL, created_at) — InnoDB utf8mb4 + 3 indexes; applied to demo_easylinksdk. Writer lib/auth-audit.ts exports recordRoleChange({actorKaryawanId,targetKaryawanId,action,roleKey,groupId},executor?=pool) — parameterised, accepts PoolConnection for tx callers. Wired into app/api/groups/route.js: assign_leader L197-207 grant, remove_leader L232-242 revoke, both via new resolveKaryawanIdByPin(pin) L15-20. Constant LEADER_ROLE_KEY='group_leader' L13. tsc clean. Build: Compiled+Lint clean; /_document export error PRE-EXISTING (verified via baseline stash). Live test admin001 -> assign+remove leader001/grp32: grant_count=1, revoke_count=1, actor=10006 non-null, role_key non-null. Seed restored. Evidence -> .omo/evidence/auth-nip/task-5-audit.txt. Gotchas (incl git-stash trap during baseline verify) -> .omo/notepads/auth-nip-reanchor-migration/learnings.md. NOT committed. NO mutation of tb_karyawan/tb_karyawan_auth/tb_karyawan_roles. NO cs_* writes. NO touch to login resolver/auth.nip/break-glass.

- `2026-06-13 02:43` — @sisyphus — Task 7 NIP placeholder block APPLIED (9990001-9990044, locked decision). Added `PLACEHOLDER_NIP_MIN/MAX` consts + `isPlaceholderEmployeeNip()` helper to lib/auth-session.ts, doc-link /tmp/nip_placeholder_report.tsv. Two guard points: app/api/auth/login/route.js L137 (NIP lane, BEFORE pw verify, returns identical 401 `Invalid credentials` to wrong-pw) + lib/auth-session.ts createAuthContextByNip L524 (returns null so /api/auth/me drops placeholders too). Added `k.nip AS karyawan_nip` to BOTH SELECTs; WHERE still on auth.nip per Oracle. Matcher digit-only regex+Number.isInteger+range: alpha NIPs admin001/employee001 → false, short seed 9999/99999 → false, suffix/decimal/negative → false. 19-case unit table all PASS (/tmp/t7-helper-unit.mjs). Live QA: placeholder QA_PH_TEST→401 byte-identical to employee001+WRONGPW→401, employee001/admin001/leader001→200. Temp seed (kar1 nip='QA_PH_TEST', is_active=1, pw='password') created+deleted, kar1 baseline (zero auth rows) restored. tsc clean, build clean, 8/8 auth-session-compat tests pass. Evidence -> .omo/evidence/auth-nip/task-7-placeholder.txt + task-7-placeholder-boundary.txt. Gotchas -> .omo/notepads/auth-nip-reanchor-migration/learnings.md. NOT committed.

- `2026-06-13` — @sisyphus — Task 3 Wave 0 preflight: created curl QA harness + Playwright login recipe + locked-decisions doc. Harness verified employee001 -> HTTP 200, subject_type=employee_nip, karyawan_id=10008. Files: .omo/evidence/auth-nip/{qa-harness.sh,qa-playwright-recipe.md,decisions.md,task-3-harness-run.txt}. No prod code touched.

- `2026-06-13 00:00` — @sisyphus — Task 2 Wave-0 preflight READ-ONLY: documented session payload shape, TTL=43200s, NIP resolve SQL, all decode branches + env flags in lib/auth-session.ts + lib/auth-hardening-helpers.js -> .omo/evidence/auth-nip/task-2-payload-shape.md
- `2026-06-12 15:30` — @sisyphus — PLACEHOLDER NIPs applied: remaining 44 NULL-nip tb_karyawan rows filled sequential 9990001-9990044 (by id, NULL-guarded) so auth/promotion tasks unblock; replace with real NIPs later by name. Result: tb_karyawan.nip now 0 NULL / 138 set (94 real-backfilled + 44 placeholder). No dup nips. Map: /tmp/nip_placeholder_report.tsv (placeholder↔name↔id). Rollback: /tmp/nip_placeholder_rollback.sql (nulls only the 44, exact-nip-guarded). Pre-state: /tmp/nip_placeholder_targets.tsv. Find placeholders: nip LIKE '999%' (NOTE seed/test rows kar10002 nip='9999', kar10004 nip='99999' are distinct, shorter). Live login still verified working post-change: leader001 NIP-lane 200 groups=[32] only (B1), admin01 account-lane 200 groups=[] (B2).
- `2026-06-12 01:00` — @sisyphus — NIP BACKFILL APPLIED to tb_karyawan from docs/agent-context/tenagaMedis_2215.csv (94 staff rows excl UMUM). Matcher /tmp/nip_match.mjs (NFKC+collapse+lowercase exact, punct-strip fallback, single-match only, nip-IS-NULL-guarded, leading-'O' kept verbatim). Applied 55 EXACT_ONE updates via /tmp/nip_backfill.sql → nip NULL 99→44, set 39→94. 12 already had matching NIP (no diffs). NO dup NIPs after. 27 UNMATCHED need user review (absent/differently-spelled in tb_karyawan): all leading-'O' radiographers (Mohammad Arief/Was'an/Dwi Kuniyanto/Sevia/Iglas), many 'dr. X, Sp.Y' specialists, Sholikin, Keny Nurchaeni C, etc — full list /tmp/nip_backfill_report.tsv (status NONE). Rollback: /tmp/nip_backfill_rollback.sql (sets those 55 ids NULL). Backups /tmp/karyawan_nip_before.tsv + /tmp/karyawan_all.tsv. 2 of 3 previously-NIP-less leaders now set: dr.Dini Pangestika kar2 grp2 nip 20211200006, Al Dilladyas kar23 grp7 nip 20211200027 (3rd Yustisia kar29 grp9 = unmatched? verify). Seed admin001/leader001/employee001 (kar10006/7/8, pw 'password') still active.
- `2026-06-12 00:30` — @sisyphus — Deployed prod REMOVED per user (still researching auth/seed). pm2 'easylink-frontend' deleted + `pm2 save` (won't respawn on reboot); /home/user/apps/easylink-frontend dir rm -rf'd; :3000 now FREE. Safety backup at /tmp/apps-easylink-backup-2026-06-12 (data/ + env.bak; the 16 'changes' were untracked .codex/skills scaffolds only). UNTOUCHED: older snapshot /home/user/apps/easylink-frontend.backup-2026-05-21 (1.2G). Single source of truth now = /home/user/projects/easylink-frontend (workspace). No server currently running on :3000.
- `2026-06-12 00:00` — @sisyphus — B1/B2 fix COMMITTED (a4aa214 on branch sync/ai-knowledge-2026-06-07): lib/auth-session.ts createAuthContextByNip per-group leader scoping + global-only admin/hr; verified eslint+tsc+28 auth unit tests. UI waves1-3 committed separately (ae052a2). Deployed pm2 'easylink-frontend' RESTORED online (cwd /home/user/apps/easylink-frontend, :3000, login 200) BUT runs OLD deployed code — commits live only in /home/user/projects workspace, NOT yet deployed to /apps. Tactical 409 DB fix still active (tb_karyawan_auth kar9999 is_active=0, shared DB so affects both copies). NOT committed: .omo/ scratch (contains session-token .cookie files — intentionally excluded from git), .multibrain/, run-continuation jsons.
- `2026-06-11 19:00` — @sisyphus — Knowledge persisted across 4 stores. Obsidian `docs/obsidian/auth-leader-schedule-map.md` rewritten with confirmed 409 root cause + B1/B2 live blockers + grill amendments (session subject=karyawan_id, drop denormalized tb_karyawan_auth.nip, k.isDeleted=0, groups UI read+write together, break-glass). Obsidian `docs/obsidian/index.md` bumped + grill added to reading order. Graphify `docs/graphify-app-direction.md` Auth section 4 rewritten for NIP-anchored direction + flowchart nodes F4/F5/F6 added. OMO `.omo/qa/auth-409-collision-diagnosis-2026-06-11.md` written (repro + DB state + tactical/strategic fix options). Multibrain context file `.multibrain/context/2026-06-11-auth-grill-and-redesign.md` written -> .multibrain/context/2026-06-11-auth-grill-and-redesign.md. No auth code touched.
- `2026-06-11 18:00` — @sisyphus — Oracle grill of NIP redesign saved -> docs/agent-context/oracle-auth-redesign-grill-2026-06-11.md. Verdict: NIP single-lane kills 409 by construction but ships 3 defects. BLOCKERS: B1 per-group is_leader broadcast across all groups (escalation, EXISTS TODAY auth-session.ts:530-545, viewer-of-B becomes leader-of-B); B2 admin role with non-null group_id → global admin (no CHECK on tb_karyawan_roles); B3 no admin break-glass (verify admin01 has tb_karyawan row first); B4 hard-cut locks 73% staff. KEY DECISIONS: session subject MUST be karyawan_id (immutable) NOT nip string (H1); drop denormalized tb_karyawan_auth.nip, resolve via tb_karyawan.nip JOIN (H2); add k.isDeleted=0 to NIP query (H3); migrate groups UI read+write together (H4); add rate-limit+CSRF (H5). STAY on tb_karyawan_roles (not cs_*) + enforce 2 guards in app code. Ship B1+B2 fix IMMEDIATELY (exploitable today, independent of redesign). 7-step dual-run migration. STOPPED here per user — no implementation yet.

- `2026-06-11 17:30` — @sisyphus — Auth redesign DB recon (live demo_easylinksdk). DECISIVE CONSTRAINT: tb_karyawan has 135 rows, **99 nip-NULL (73%)**, 36 nip-present, 128 pin-present. tb_karyawan_auth requires nip NOT NULL UNIQUE (PK=karyawan_id, FK→tb_karyawan.id). So "bind login on NIP" only works for 27% of staff unless NIP backfilled — robust canonical key is karyawan_id (PK, always present), not NIP string. Current leaders live in tb_user_group_access (pin-keyed, fragile, machine-derived): pin82→kar2(nip NULL,grp2), pin65→kar23(NULL,grp7), pin17→kar29(NULL,grp9), pin99→kar108(nip 20250700001,grp9). Group 9 has TWO leaders (17+99) → multiple-leaders-per-group ALREADY works in tb_user_group_access. Canonical target tb_karyawan_roles(id,karyawan_id,role_key enum('admin','hr','group_leader','scheduler','viewer'),group_id) supports N leaders/group too but currently holds only 1 row (kar9999=admin,grp NULL). tb_employee_group PK=karyawan_id → employee in exactly ONE group (1:1). Plan: move leader/schedule capability source-of-truth from tb_user_group_access.is_leader/can_schedule (pin) → tb_karyawan_roles (karyawan_id). DECISION PENDING from user: NIP-string key vs karyawan_id key given 73% NIP gap. NOT YET IMPLEMENTED.
 - `2026-06-13 02:51` — @sisyphus-junior — Task 6 H5: login rate-limit (10/60s ip+loginId) + unified 401 credential error; gates green, 31 tests pass
