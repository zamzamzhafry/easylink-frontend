node_modules/
.env
.env.local
*.sql
!auth_group_shift_revision.sql
!migration_v3_clean_slate_schema.sql
!migration_v3_clean_slate_rollback.sql
!migration_machine_user_polling.sql
!scripts/migration-task-*.sql
!migrations/*.sql
*.tsbuildinfo
# .sisyphus/ intentionally tracked — AI knowledge (sessions, plans, notepads)
.docs/
*.sql.example
nul
Postman/
.next/
*.log
.playwright-mcp/
.graphify/
# Large SDK binaries and dumps
docs/learning/sdk-share-dumps/
tmp/
test-results/
# Multi-brain port registry runtime (machine-local, per-session leases)
.multibrain/port-registry.jsonl
.multibrain/port-registry.lock

# Session cookie jars + live tokens (never commit)
*.cookie
.omo/logs/*cookie*
.omo/evidence/*cookie*
.omo/evidence/*.cookie
.env.example.local

# Knowledge scratch — cookie jars / live-token evidence (never commit, public repo)
.omo/
.sisyphus/logs/
.sisyphus/evidence/
.sisyphus/run-continuation/
dogfood-output/
.claude/scheduled_tasks.lock
